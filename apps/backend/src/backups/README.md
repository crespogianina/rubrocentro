# Módulo `backups`

Estado: **no arrancado todavía.**

Backup local 3-2-1, verificación de integridad y registro en backup_log. Ver Stage 10 de TASKS.md.

Misma estructura que `catalogo/` (domain → application → infrastructure →
presentation) — ver ese módulo como plantilla del patrón antes de empezar
este.
